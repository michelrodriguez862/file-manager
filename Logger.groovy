class Logger {
    // Método que registrará los mensajes
    def log(String message) {
        // Para simplicidad, se muestra el mensaje en la consola.
        // Se puede redirigir a un archivo si se necesita.
        println "[LOG] " + message
    }
}
